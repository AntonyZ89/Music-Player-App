import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {},
  imageContainer: {
    marginTop: 100,
    alignItems: 'center',
  },
  image: {
    width: 300,
    height: 300,
  },
});
